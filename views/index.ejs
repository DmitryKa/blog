<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap.min.css' />
  </head>
  <body>
    <h1>Blog</h1>
    <% for(i = 0; i < articles.length; i++) {%>
        <div><strong><%= articles[i].title %></strong></div>
        <div><%= articles[i].text %></div>
    <% } %>
  </body>
</html>
